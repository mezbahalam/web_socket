source 'http://rubygems.org'



gem 'rails', '4.1.8'

gem 'sqlite3'
gem 'sqlite3-ruby'
gem 'sass-rails', '~> 4.0.3'
gem 'uglifier', '>= 1.3.0'
gem 'coffee-rails', '~> 4.0.0'
gem 'jquery-rails'
gem 'turbolinks'
gem 'jbuilder', '~> 2.0'
gem 'sdoc', '~> 0.4.0',          group: :doc
# Windows does not include zoneinfo files, so bundle the tzinfo-data gem
gem 'tzinfo-data', platforms: [:mingw, :mswin]

#for websocket

gem "haml-rails"
gem "bootstrap-sass"
gem "autoprefixer-rails" #support for bootstrap
gem "puma" # different kind of server that support treading
gem "devise"
gem "faye-websocket" #core of websocket
gem "pry" #debug
gem "hirb" #table layout in the console

group :test do
  gem "database_cleaner"
  gem "timecop"
  gem "pry-byebug"
end

